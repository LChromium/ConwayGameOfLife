# 阶段 C：大棋盘基准

**范围**：只做基准。**不**扩展播种功能，**不**改 CPU 单生成器，**不**做亚像素密度总览
（评审明确：等出现真实显示问题再另行实施），**不**做 GPU 独立噪声（远期可选实验）。
本阶段**不据此指定任何优化方向**——理由见 §9。

规格要求「分别记录生成、上传、演化、显示与内存占用」，因此全文**五个口径分开成表**，
不合并、不相加。合起来看一个数，正是阶段 A 明确拒绝过的那种归因。

---

## 0. 本轮修正（相对 r1）

第一轮（`stage-c-bench.jsonl`，提交 `f87ae32`）的**原始记录原样保留、没有重写**；
下面是报告里被评审指出、已经改掉的地方。r2 记录在 `stage-c-bench-r2.jsonl`，
每条带 `recordRound: 2`。

| # | r1 的说法 | 问题 | r2 的做法 |
|---|---|---|---|
| 1 | 用约 0.06 ms 的 GPU 帧时间解释演化成本 | 那次采样是**暂停场景**；且把 CPU 提交开销（0.001 ms）当成了 GPU 工作量 | 删除该归因。GPU 帧时间按**四个分别标注的场景**采样；每个场景报**有效样本数 / 请求帧数**（实测 17–25 / 120–200，不是 120 个有效样本） |
| 2 | 「运行中」场景 | 传了 `Repaint.EveryFrame`，测的是「演化＋每帧强制重绘」；而且固定随机盘面**只加载到临时后端**，跨帧测量用的 `active` 后端可能还在跑默认样本 | 正常运行场景**取消额外强制重绘**（应用自己每代重绘一次）；跨帧测量前把指定盘面加载进 `active` 后端、设环绕边界、记起始世代；「暂停＋每帧重绘」保留为独立的压力场景 |
| 3 | GPU 同步数字跨尺寸比较 | 每批步数 N 随尺寸变化（20/5/5/2），回读成本被不同比例摊到每代 | 改名 **`gpuBatchAdvancePlusOneReadbackAmortisedMsPerGeneration`**，记 `batchSteps`，**N 统一为 5**；并写明回读占比**未分项测量** |
| 4 | 「16 B/格＝两份状态＋两份驱动暂存」 | 只测了图形驱动计数的总增量，看不见内部分配；(负托管增量也不该归因于 GC 机制) | 只保留可证的：两份显式状态缓冲 **8 B/格**（源码）＋**实测驱动增量**；差额标 **未归因**。「31 B/格总计」降级为**所列缓冲的容量小计**，并列出它漏掉了什么 |
| 5 | 「CPU 后端每次重绘都要花这笔成本」 | `LifeGridElement` 用 `uploadPending` 门控，平移/缩放/普通刷新不触发；且它是 CPU 数组读取＋格式转换，不是 GPU→CPU 回传 | 限定为「**棋盘变化且使用 CPU 后端时**的整盘复制＋上传」，并在记录里写 `IsGpuReadback: false` 与触发条件 |
| 6 | 「帧时间都不变」 | 中位数接近，但均值明显更高，尾部有长帧 | 改为「**中位数接近；均值与尾部更高，尚未归因**」，并给出均值、最大值与首/末十分位中位数 |

另外两处工具自身的问题也一并修了：图形驱动计数不可用时不再输出 `0 − 8 B/格` 这种
等于「驱动什么都没多分配」的假数字（改为 `null`）；`LifeGridElement.LastUploadMilliseconds`
是**粘性**的，所以「非零帧数」**不是上传次数**，字段已改名并附说明。

---

## 1. 测量契约

每个数字是什么、包含什么、不包含什么：

| 口径 | 测的代码路径 | 含 | 不含 |
|---|---|---|---|
| **生成** | `LifeNoiseSeeding.Generate`（CPU、托管、同步） | 一次完整噪声盘面 | 上传、演化、显示 |
| **上传** | `ILifeBackend.LoadBoard` | CPU 后端：逐活细胞 `SetCell`（人口增量维护，是应用真实路径）；GPU 后端：暂存 `uint[]` 拷贝 + `ComputeBuffer.SetData` | 噪声生成、传输完成 |
| **演化** | `ILifeBackend.Step` | CPU 单步；GPU 仅提交；GPU **批量推进 N=5 代 + 一次整盘回读，再除以 5** | 初态生成、上传、显示 |
| **显示** | `LifeGridElement.Refresh`；CPU 后端棋盘变化时的整盘复制＋上传；**四个标注场景**的帧间隔、`FrameTimingManager` 帧时间、视口事实 | 提交侧派发＋背景赋值；帧间隔是真实跨墙钟；GPU 帧时间是**整帧**的 GPU 时间 | 演化（帧间隔里除外，并单独报先进了多少代） |
| **内存** | `GC.GetTotalMemory`、`Profiler.*`，以及**按源码列出的**缓冲容量 | 本进程内**多分配一块同尺寸棋盘**并释放的增量；所列缓冲的容量小计 | 内部分配的归因；不是应用存活总量，也不是峰值 |

**四条口径纪律**（前三条例自阶段 A，本轮不放松，第四条是本轮新增）：

1. **GPU「仅提交」不是 GPU 执行时间。** 0.0006–0.0015 ms 是把手上的活交给 GPU 的代价。
2. **批量推进＋回读的数字是摊销值**，含一次整盘回读，回读占比**未分项测量**；
   与 CPU 单步不是同一个操作（CPU 就地算、不回读），**不推导任何 CPU/GPU 加速比**。
3. **`SetData` 不是传输完成时间**，上传数字不构成带宽声明。
4. **帧间隔不是纯计算成本**：关掉 vsync 与限帧只是让它描述工作量而非节拍，
   调度、GC、驱动提交都在里面。

---

## 2. 环境与配置（每条记录自带）

| 项 | 值 |
|---|---|
| CPU / GPU / API | AMD Ryzen 7 7800X3D（16 线程） / NVIDIA GeForce RTX 4070 Ti（11994 MB） / Direct3D12 |
| 系统内存 | 31937 MB |
| 窗口 / 网格视口 | 900×700 窗口，网格视口 **615×456 像素**，每格 1 像素，边界环绕 |
| 跨帧场景的初始盘面 | **固定随机盘面，seed 20260915，密度 0.30，加载进 `active` 后端**，环绕边界，世代从 0 开始（记录里的 `frameScenarioBoard` 字段逐字说明） |
| 帧场景条件 | `vSyncCount = 0`、`targetFrameRate = −1`、`Application.runInBackground = true`；每条记录带 `focusedThroughout` |
| Unity | 6000.6.0f1，IL2CPP |

三种构建设置，每条记录用 `buildGuid` + `dataPath` 自报身份：

| 构建 | 输出 | 帧时间可用 | 图形驱动计数可用 |
|---|---|---|---|
| 帧时间构建 | `Builds/LifeTerminal-bench.exe`（构建时打开 `enableFrameTimingStats`） | **是** | 否 |
| 发布构建 | `Builds/LifeTerminal-release.exe`（`BuildOptions.None`） | 是 | 否 |
| 主测量构建（r1 用） | `Builds/LifeTerminal.exe` | 是 | **是** |

> 两点如实说明。其一，`Debug.isDebugBuild` 在 Unity 6 的这些构建里报 **false**，
> 即使 `BuildOptions.Development` 生效（编辑器对 `DEVELOPMENT_BUILD` 宏也给了弃用警告），
> 所以记录里保留 `isDevelopmentBuild` 字段但**不拿它当作"是不是开发版"的判据**；
> 真正可核对的是各字段自己报的可用性。其二，**图形驱动计数在 r2 的构建里全是 0**，
> 因此「实测驱动增量」引自 **r1 记录**（`MeasureMemory` 那段代码两轮相同，只有字段名变了），
> 在 §7 里逐条标明出处，不与 r2 的数字混排。

### 一次真实事故：失焦冻结

第一次批量运行时，有一轮墙钟耗时 **778 秒**，而该阶段内部 5 个采样全部 ≤ 96 ms。
逐阶段计时定位到 `upload-cpu` 阶段被冻结在中间。**原因是窗口失焦**——独立 Player 默认
`Application.runInBackground = false`，失焦即停走，而 `Time.realtimeSinceStartup` 照常计墙钟。
（阶段 A 的 T15 记过同一现象，当时只留下「与失焦相关」的现场观察，没有定因。）
现在基准开跑前强制 `runInBackground = true`，并把 `focused` 写进每条阶段日志。

---

## 3. 生成（CPU 噪声）

| 盘面 | fBm 中位 | fBm 吞吐 | 均匀中位 | 均匀吞吐 | fBm 实际密度 | 均匀实际密度 |
|---|---|---|---|---|---|---|
| 256²（65 536） | 34.8 ms | 1.88 M 格/s | 1.3 ms | 50 M 格/s | 0.3189 | 0.3202 |
| 1024²（1 048 576） | 559.9 ms | 1.87 M 格/s | 12.2 ms | 86 M 格/s | 0.3101 | 0.3198 |
| 2048²（4 194 304） | 2 229 ms | 1.88 M 格/s | 50.1 ms | 84 M 格/s | 0.3143 | 0.3200 |
| 4096²（16 777 216） | 8 918 ms | 1.88 M 格/s | 229.6 ms | 73 M 格/s | 0.3197 | 0.3201 |

**fBm 每格成本与尺寸无关**（1.87–1.88 M 格/s，跨 256 倍格数）：纯函数、无分辨率依赖。
1024² 的 559.9 ms 与阶段 B 记录的 528–563 ms 相符，实际密度 0.3101 也与阶段 B 相同。
均匀模式请求 0.32，实测 0.3198–0.3202。

> **4096² 的 8.9 秒是评审点名的最有价值的发现之一**：它就是打开大棋盘要等的交互时间。

---

## 4. 上传

| 盘面 | CPU `LoadBoard` 中位 | GPU `LoadBoard` 中位 | GPU 吞吐 |
|---|---|---|---|
| 256² | 0.40 ms | 0.17 ms | 390 M 格/s |
| 1024² | 5.26 ms | 1.43 ms | 735 M 格/s |
| 2048² | 23.20 ms | 5.27 ms | 797 M 格/s |
| 4096² | 92.63 ms | 25.20 ms | 666 M 格/s |

两者都随格数线性。CPU 侧不是 `memcpy`：`LoadBoard` 逐活细胞调 `SetCell`，
人口是增量维护的（应用真实走的路）。GPU 侧含暂存拷贝与 `SetData`，不含传输完成。

---

## 5. 演化

| 盘面 | CPU 单步中位 | CPU 吞吐 | GPU 仅提交 | GPU 批量推进+一次回读（N=5，摊销） |
|---|---|---|---|---|
| 256² | 2.88 ms | 22.8 M 格/s | 0.0007 ms | 0.055 ms |
| 1024² | 40.06 ms | 26.2 M 格/s | 0.0006 ms | 0.559 ms |
| 2048² | 164.4 ms | 25.5 M 格/s | 0.0007 ms | 2.252 ms |
| 4096² | 649.6 ms | 25.8 M 格/s | 0.0010 ms | 7.272 ms |

- CPU 每格成本恒定（22.8–26.2 M 格/s），与阶段 A 的 38.66 ms @1024² 同一量级（本轮 40.06 ms）。
- GPU 仅提交**与盘面大小无关**：一次派发就是一次派发。
- 摊销列按定义含**一次整盘回读**，占比**没有分项测量**，所以它随盘面增长有多少来自回读、
  多少来自执行，本阶段**不知道**。N 统一为 5，跨尺寸才可比。
- **不推导加速比**：CPU 就地算、不回读，两者不是同一个操作。

---

## 6. 显示

### 6.1 两个固定成本

| 盘面 | 渲染派发（GPU 后端） | CPU 后端：棋盘变化时的整盘复制＋上传 |
|---|---|---|
| 256² | 0.0024 ms | 0.56 ms |
| 1024² | 0.0031 ms | 6.69 ms |
| 2048² | 0.0032 ms | 25.47 ms |
| 4096² | 0.0038 ms | 108.35 ms |

- 渲染派发是提交侧派发＋背景赋值，**不是 GPU 执行时间**；渲染 kernel 写的是视口大小的纹理
  （615×456），与盘面大小无关。
- 第二列是**托管拷贝（含 0/1 归一化）＋ `ComputeBuffer.SetData`**，**不是 GPU→CPU 回传**；
  它只在**棋盘变化且 CPU 后端在用**时发生（`LifeGridElement` 用 `uploadPending` 门控，
  平移/缩放/普通刷新不触发）。记录里带 `IsGpuReadback: false` 与触发条件。
- 这一列的数值是**组件测量**；§6.2 的第 4 个场景里，应用自己上报的数字（99–120 ms @4096²）
  与它一致。

### 6.2 四个分别标注的帧场景

每个场景单独采样，GPU 帧时间**只报有效样本数**，不把请求帧数当样本数。

| 盘面 | 场景 | 强制重绘 | 请求/达成 代/秒 | 帧中位 | 帧均值 | 帧最大 | 末十分位中位 | GPU 帧时间（有效样本/请求） |
|---|---|---|---|---|---|---|---|---|
| 256² | 暂停 | 否 | — | 0.285 ms | 0.490 | 2.82 | 1.89 | 0.049 ms（20/120） |
| 256² | 暂停＋每帧重绘 | **是** | — | 0.278 ms | 0.474 | 2.65 | 2.01 | 0.056 ms（20/120） |
| 256² | **正常运行** | 否 | 20 / 19.99 | 0.275 ms | 0.432 | 5.05 | 1.29 | 0.041 ms（21/200） |
| 256² | 正常运行（CPU 后端） | 否 | 20 / 19.98 | 0.287 ms | 0.500 | 6.80 | 1.45 | 0.041 ms（19/200） |
| 1024² | 暂停 | 否 | — | 0.314 ms | 0.554 | 2.73 | 1.93 | 0.070 ms（21/120） |
| 1024² | 暂停＋每帧重绘 | **是** | — | 0.305 ms | 0.506 | 2.75 | 1.83 | 0.061 ms（20/120） |
| 1024² | **正常运行** | 否 | 20 / 19.99 | 0.278 ms | 0.463 | 5.79 | 1.33 | 0.041 ms（19/200） |
| 1024² | 正常运行（CPU 后端） | 否 | 12 / 11.91 | 0.264 ms | 0.974 | 59.4 | 1.41 | 0.043 ms（19/200） |
| 2048² | 暂停 | 否 | — | 0.291 ms | 0.631 | 5.19 | 2.92 | 0.059 ms（17/120） |
| 2048² | 暂停＋每帧重绘 | **是** | — | 0.296 ms | 0.553 | 3.78 | 2.02 | 0.061 ms（22/120） |
| 2048² | **正常运行** | 否 | 20 / 20.00 | 0.294 ms | 0.542 | 8.98 | 1.65 | 0.041 ms（19/200） |
| 2048² | 正常运行（CPU 后端） | 否 | 3 / 2.90 | 0.288 ms | 1.286 | 209.4 | 1.45 | 0.041 ms（19/200） |
| 4096² | 暂停 | 否 | — | 0.351 ms | 0.811 | 9.46 | 3.05 | 0.047 ms（20/40） |
| 4096² | 暂停＋每帧重绘 | **是** | — | 0.282 ms | 0.731 | 12.28 | 1.80 | 0.054 ms（20/40） |
| 4096² | **正常运行** | 否 | 20 / 19.99 | 0.276 ms | 0.497 | 6.91 | 1.44 | 0.041 ms（17/200） |
| 4096² | 正常运行（CPU 后端） | 否 | 1 / 0.88 | 0.309 ms | 1.298 | 799.4 | 1.83 | 0.041 ms（20/200） |

怎么读这张表：

- **中位数在四个尺寸、四个场景之间都接近（0.26–0.35 ms）；均值明显更高（0.43–1.30 ms），
  尾部更长（最大 2.7–799 ms）。这些长尾本阶段没有归因**，也不打算在这份报告里追。
- 关掉限帧只说明这些间隔**描述工作量而非节拍**，**不等于纯计算成本**。
- GPU 帧时间是**整帧**的 GPU 时间（含 UI 合成），有效样本 17–25 个，**不足以支撑**
  「演化成本随盘面不变」这类结论——r1 那样用它解释演化已经删除。
- CPU 后端的运行场景**请求速率按后端能力下调**（20 / 12 / 3 / 1 代/秒），原因见 §6.4；
  两个 `max` 值（209 ms、799 ms）就是该尺寸下**一次 CPU 单步**落在某一帧里。
- 「正常运行（CPU 后端）」场景里，应用自己上报的上传成本中位数为
  0.43 / 5.33 / 99.3 / 120.0 ms（记录字段 `gridReportedUploadMs`），与 §6.1 第二列一致。

### 6.3 棋盘大于视口时，看不到的部分就是不显示

| 盘面 | 每格像素 | 可见格数 | 占盘面 |
|---|---|---|---|
| 256² | 1 | 256×256 | **100%**（整盘可见） |
| 1024² | 1 | 615×456 | 26.7% |
| 2048² | 1 | 615×456 | 6.7% |
| 4096² | 1 | 615×456 | **1.67%** |

缩放是**整数**像素/格且**不小于 1**，盘面永远不会被降采样：大于视口的棋盘只显示居中的一块。
这是显示路径的既有设计（保证「一个活细胞永远不小于一个像素」），本阶段不改它，只量化代价。

> 评审点名这是第二个最有价值的发现：**4096² 时 98.3% 的盘面不可见**。

### 6.4 一个产品级观察：CPU 后端在大棋盘上的时钟追赶

基准第一次尝试在「CPU 后端 + 速度滑块 20 代/秒」下运行 2048² 与 4096² 时，
Player **挂死**（一次 318 秒后崩溃、一次 600 秒超时仍未结束）。逐阶段日志把它定位在
该场景的第一帧之后。

机制（读代码可核对，未单独埋点）：控制器 `Update` 用
`while (accumulator >= interval) backend.Step()` 追赶，`interval` 是滑块速率的倒数
（20 代/秒 → 50 ms）。2048² 的**一次 CPU 单步约 150–165 ms**、4096² 约 650 ms，
都**远大于** 50 ms，于是欠账在同一帧内越滚越大：一帧内 7 步 → 21 步 → 64 步 → …
帧永远结束不了，主线程不再返回。

- **能说的**：在这台机器上，CPU 后端在 2048²/4096² 达不到滑块最高速率，
  而且失败方式是**主线程不返回**，不是「帧率低」。
- **不能说的**：没做受控实验，因此不声称这是唯一原因；也没有测量用户在多慢的速率下会先感知到卡顿。
- **基准的处理**：该场景改为请求后端能承受的速率（20 / 12 / 3 / 1 代/秒，
  取 CPU 单步中位数倒数的二分之一并夹在滑块范围内），并把请求速率写进记录。
  这样测到的是**稳态**显示成本，而不是把追赶崩溃当成数据。
- **交给评审的**：这是一个与「8.9 秒生成等待」同族的**交互问题**，属于产品行为而非基准口径；
  本阶段按指示只记录、不修。

---

## 7. 内存

### 7.1 所列缓冲的容量小计（源码）

| 缓冲 | 每格 | 256² | 1024² | 2048² | 4096² |
|---|---|---|---|---|---|
| CPU 状态（`current` + `next`） | 2 | 131 072 | 2 097 152 | 8 388 608 | 33 554 432 |
| GPU 状态（front + back） | 8 | 524 288 | 8 388 608 | 33 554 432 | 134 217 728 |
| GPU 暂存数组（上传 + 回读） | 8 | 524 288 | 8 388 608 | 33 554 432 | 134 217 728 |
| 显示上传缓冲 | 4 | 262 144 | 4 194 304 | 16 777 216 | 67 108 864 |
| 显示上传暂存 | 4 | 262 144 | 4 194 304 | 16 777 216 | 67 108 864 |
| 基准自己的盘面 + 回读池 | 5 | 327 680 | 5 242 880 | 20 971 520 | 83 886 080 |
| **小计** | **31** | **2.03 MB** | **32.5 MB** | **130 MB** | **520 MB** |

**这是「本探针列出的缓冲的容量小计」，不是应用的存活总量，也不是峰值。** 它漏掉了：
播种会话的两个盘面数组、控制器保存的初态与样本盘面、渲染器的视口纹理等；
同时把只在测量期间存在的回读池算进来了。记录字段名叫 `listedBufferCapacityBytes`
（不是 r1 的 `computedBytes`），并带同样的说明。

### 7.2 实测增量

| 盘面 | CPU 盘面（托管，r2 实测） | GPU 盘面（图形驱动，**r1 实测**） |
|---|---|---|
| 256² | 139 264 B | 786 432 B（≈12 B/格） |
| 1024² | 2 105 344 B | 16 777 216 B（≈16 B/格） |
| 2048² | 8 396 800 B | 67 108 864 B（≈16 B/格） |
| 4096² | 33 422 336 B | 268 435 456 B（≈16 B/格） |
- **CPU 盘面实测 ≈ 2 B/格**（2.00–2.13），与两个 `byte[]` 一致。
- **GPU 盘面驱动增量：r2 的构建里图形驱动计数全是 0**（记录里 `graphicsDriverMemoryAvailable: false`
  并写明原因），因此这一列引自 **r1 记录**（同一段 `MeasureMemory` 代码，只有字段名不同）。
- 能确认的只有两件事：**两份显式状态缓冲合计 8 B/格**（源码）；
  **本机、本 API、本次测量时点，驱动计数增量约 12–16 B/格**。
  **额外部分来源尚未定位**——r1 曾把它写成「两份上传暂存」与「小尺寸共享池」，那两条确定性
  归因已删除，现在记录里是 `driverDeltaMinusStateBuffersBytes` + 「未归因」的说明。
- **托管增量不可作为分配量度**：小尺寸出现负值，且不跟随实际分配的数组。
  r1 曾把它归因于「非压缩 GC 保留大块」；**该机制未经证实，已删除**，
  现在只标注「不可用、原因未确认」（`managedDeltaUsable: false`）。

### 7.3 进程整体（仅供参考，不参与结论）

开发版配置下 `allocatedTotalMB` 166–239 MB（r1）、327–479 MB（r2，构建差异未追），
`reservedTotalMB` 578–839 MB。**这两组数不跨构建比较**，也不用来对账 §7.1 的小计。

---

## 8. 发布配置交叉检查

r2 的发布构建（`BuildOptions.None`）跑了两端尺寸，与帧时间构建逐项对比：

| 项 | 256² 帧时间构建 | 256² 发布构建 | 4096² 帧时间构建 | 4096² 发布构建 |
|---|---|---|---|---|
| fBm 生成 | 34.8 ms | 35.8 ms | 8 918 ms | 8 969 ms |
| 均匀生成 | 1.3 ms | 1.1 ms | 229.6 ms | 215.6 ms |
| CPU 上传 | 0.40 ms | 0.29 ms | 92.6 ms | 87.0 ms |
| GPU 上传 | 0.17 ms | 0.15 ms | 25.2 ms | 22.7 ms |
| CPU 单步 | 2.88 ms | 2.20 ms | 649.6 ms | 655.4 ms |
| GPU 仅提交 | 0.0007 ms | 0.0009 ms | 0.0010 ms | 0.0015 ms |
| GPU 批量+回读（N=5） | 0.055 ms | 0.089 ms | 7.27 ms | 9.77 ms |
| 渲染派发 | 0.0024 ms | 0.0023 ms | 0.0038 ms | 0.0024 ms |
| 正常运行帧中位 | 0.275 ms | 0.290 ms | 0.276 ms | 0.265 ms |
| 正常运行达成代/秒 | 19.99 | 19.99 | 19.99 | 19.99 |

**量级与趋势一致，差异在几个百分点之内**（最大差异出现在小尺寸的亚毫秒项，
如 GPU 上传 0.17 vs 0.15 ms）。这一条只说明「**测量配置没有把数字变好看**」，
**不构成发布版性能验收**：跑的是 900×700 窗口、一块 RTX 4070 Ti、D3D12，
而且 r2 里没有一个构建能报图形驱动内存。

---

## 9. 本阶段**不**得出的结论

- **不指定优化方向。** 五个口径分开记录，但「该优化哪一个」由评审按目标决定；
  本阶段没有做任何优化或前后对比。
- **不声称 CPU/GPU 加速比**；**不把 GPU 仅提交当执行时间**；
  **不分解批量摊销里的回读占比**。
- **不用暂停场景的 GPU 帧时间解释演化**（r1 的这条已删除）。
- **不声称「帧时间与盘面无关」**：只有中位数接近，均值与尾部未归因。
- **不做亚像素密度总览**：只给出「4096² 时 98.3% 盘面不可见」这个数字。
- **不改 CPU 单生成器**：生成仍是「CPU 生成一次、批量上传」。

### 交给评审的产品观察（本阶段只记录，不实施）

1. **4096² 的 fBm 生成约 8.9 秒**：这是打开大棋盘的真实交互等待。评审已指明它是后续优化的
   明确候选，且**先记录真实交互等待与取消响应**再决定手段——本阶段没有测这两项。
2. **4096² 时 98.3% 的盘面不可见**：评审支持随后做**全盘密度总览＋局部细胞观察**
   （总览解决"看不到整个实验"，局部视图保留逐格观察与编辑）。这是**显示能力完善**，
   **不应宣传为演化算法提速**。
3. **CPU 后端在大棋盘上达不到高时钟速率，且失败方式是主线程不返回**（§6.4）。

### 已知限制

| 限制 | 说明 |
|---|---|
| 单机单配置 | 7800X3D / RTX 4070 Ti / D3D12，一种窗口（视口 615×456）。换机器或换视口，显示一栏会变 |
| 帧时间有效样本少 | 每场景 17–25 个（请求 120–200）。记录里两个数都给，不外推 |
| 图形驱动内存只有 r1 有 | r2 的构建里计数为 0；§7.2 已标明出处 |
| 大尺寸采样次数少 | 生成与演化：256²/1024² 5 次重复，2048² 3 次，4096² 2 次；批量步数统一 5。数字的 `samples` 与 `batchSteps` 都写在记录里 |
| 托管内存增量不可用 | 见 §7.2，不作为依据 |
| 每帧重绘是人为最坏情况 | 应用实际每代重绘一次；该场景只用来分离「重绘」与「演化」 |

---

## 10. 复现

```powershell
# 帧时间构建（构建时打开 enableFrameTimingStats）与发布构建
unity build . --target StandaloneWindows64 -o Builds\LifeTerminal-bench.exe `
    --execute-method ConwayGameOfLife.EditorTools.PlayerBuild.BuildBenchmarkWindows64
unity build . --target StandaloneWindows64 -o Builds\LifeTerminal-release.exe `
    --execute-method ConwayGameOfLife.EditorTools.PlayerBuild.BuildReleaseWindows64

# 每个尺寸一个进程：分配器与峰值都是干净的
foreach ($size in '256x256','1024x1024','2048x2048','4096x4096') {
    Builds\LifeTerminal-bench.exe -screen-width 900 -screen-height 700 -screen-fullscreen 0 `
        -lifeBoard $size -lifeBench
}
```

- `stage-c-bench-r2.jsonl`：r2 记录（6 条：帧时间构建 ×4 尺寸，发布构建 ×2 尺寸），每条带
  `recordRound: 2`、`buildGuid`、`dataPath` 与各字段自己的可用性说明。
- `stage-c-bench.jsonl`：r1 记录（10 条），**原样保留、没有重写**；§0 列出被修正的解释，
  §7.2 的驱动内存数字引自这里。
- Player 日志里的 `[stage-c-bench] phase <名字>: <秒>s focused=<真假>` 是逐阶段时间线：
  失焦冻结（§2）与 CPU 追赶崩溃（§6.4）都是靠它定位的。
